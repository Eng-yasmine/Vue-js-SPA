const { createApp } = Vue;

createApp({
    data(){
        return {
            name : 'yasmeen',
            age : 30,
            position : 'full stack developer',
        }

    }
}).mount('#app-root');